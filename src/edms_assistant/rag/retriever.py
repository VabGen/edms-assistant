import logging
from typing import List, Dict, Any
from pathlib import Path
import pickle

from langchain_openai import ChatOpenAI
from edms_assistant.core.settings import settings
from src.edms_assistant.rag.hybrid_search import HybridSearch

logger = logging.getLogger(__name__)


async def _expand_and_route_query(question: str, chat_history: List[Dict]) -> str:
    """
    Расширяет пользовательский запрос с учётом:
    - контекста из истории чата,
    - официальной терминологии СЭД и корпоративных документов,
    - синонимов и родовых понятий.

    Цель: максимизировать recall при поиске в гетерогенных документах.
    """
    # Берём последние 2 вопроса пользователя для контекста
    history = " ".join(
        msg["content"] for msg in chat_history[-2:] if msg["role"] == "user"
    ) if chat_history else ""

    input_text = f"Контекст предыдущих вопросов: {history}\nТекущий вопрос: {question}" if history else question

    llm = ChatOpenAI(
        api_key="not-needed",
        base_url=str(settings.vllm.generative_base_url),
        model=settings.vllm.generative_model,
        temperature=0.1,  # Немного креатива для синонимов
        max_tokens=128
    )

    # Системная инструкция для расширения запроса
    system_prompt = (
        "Ты — модуль семантического расширения запросов для корпоративной системы документооборота (СЭД). "
        "Твоя задача — преобразовать пользовательский вопрос в поисковый запрос, "
        "максимально релевантный для внутренних документов организации. "
        "Учитывай все типы документов: регламенты, инструкции, приказы, формы, таблицы, PDF-справочники."
    )

    user_prompt = f"""Правила:
1. Сохрани исходный смысл вопроса.
2. Добавь официальные термины (например: «бумага» → «документ», «подписать» → «визировать», «согласовать» → «пройти согласование»).
3. Включи синонимы, родовые понятия и возможные формулировки из делопроизводства.
4. Для таблиц: добавь «таблица», «сводка», «перечень».
5. Удали разговорные выражения, исправь опечатки.
6. Ответ должен быть на русском языке, КОРОТКО, БЕЗ пояснений — только поисковый запрос.

Примеры:
Вопрос: Как оформить отпуск?
Запрос: оформление заявления на ежегодный оплачиваемый отпуск, приказ о предоставлении отпуска

Вопрос: Где найти список ответственных?
Запрос: таблица ответственных лиц, перечень сотрудников с ролями, реестр ответственных

Вопрос: {input_text}
Запрос:"""

    resp = await llm.ainvoke([
        ("system", system_prompt),
        ("user", user_prompt)
    ])
    return resp.content.strip()


async def retrieve_and_generate(
        question: str,
        filename: str,
        chat_history: List[Dict[str, Any]],
        vector_store
) -> str:
    """
    Выполняет:
    1. Расширение запроса,
    2. Гибридный поиск (семантика + ключевые слова),
    3. Генерацию ответа на основе найденного контекста.

    Поддерживает все типы документов: текст, таблицы, структурированные формы.
    """
    logger.debug(f"💬 История чата (из Redis): {chat_history}")

    # Шаг 1: Расширяем запрос
    enriched_query = await _expand_and_route_query(question, chat_history)
    logger.debug(f"🔍 Расширенный запрос: {enriched_query}")

    # Шаг 2: Загружаем чанки из диска
    store_dir = Path(settings.paths.vector_stores_dir) / Path(filename).stem
    chunks_path = store_dir / "chunks.pkl"

    if not chunks_path.exists():
        logger.error(f"❌ Чанки не найдены: {chunks_path}")
        return "REFLECT: Документ не проиндексирован"

    with open(chunks_path, "rb") as f:
        chunks = pickle.load(f)

    # Шаг 3: Гибридный поиск
    hybrid = HybridSearch(vector_store, chunks)
    results = hybrid.search(query=enriched_query, k=5)
    relevant_docs = [doc for doc, _ in results[:3]] if results else []

    if not relevant_docs:
        return "REFLECT: Релевантная информация не найдена"

    # Шаг 4: Формируем контекст с метаданными
    context_parts = []
    for doc in relevant_docs:
        source = doc.metadata.get('source', 'Неизвестный источник')
        doc_type = doc.metadata.get('type', 'text')

        if doc_type == 'table':
            content = f"ТАБЛИЦА из {source}:\n{doc.page_content}"
        else:
            content = f"ДОКУМЕНТ {source}:\n{doc.page_content}"

        context_parts.append(content)

    context = "\n\n---\n\n".join(context_parts)
    logger.debug(f"📚 Контекст:\n{context}")

    # Шаг 5: Формируем историю диалога
    history_str = "\n".join(
        f"[{m['role'].upper()}]: {m['content']}" for m in chat_history
    ) if chat_history else "История отсутствует."

    # Шаг 6: Системный промпт — строгая роль для LLM
    system_prompt = f"""Ты — официальный эксперт по внутренним документам организации. 
Ты работаешь с разнообразными типами документов: регламентами, инструкциями, приказами, формами, таблицами (Excel), PDF-справочниками.

Строгие правила генерации ответа:
1. Отвечай ИСКЛЮЧИТЕЛЬНО на основе предоставленного КОНТЕКСТА ниже.
2. НЕ используй внешние знания, НЕ интерпретируй, НЕ выдумывай.
3. Если информация отсутствует — ответь ТОЧНО: «Я не нашёл информацию в документах».
4. Если вопрос не относится к документам — ответь: «Этот вопрос не относится к внутренним документам организации».
5. Для таблиц: сохраняй структуру данных, поясняй колонки при необходимости.
6. Сохраняй официальный стиль и точные формулировки из документов.
7. Указывай источник (имя файла) при цитировании.
8. Отвечай на русском языке, полно и детально.

Контекст (документы):
{context}"""

    # Шаг 7: Пользовательский промпт
    user_prompt = f"""История диалога:
{history_str}

Текущий вопрос пользователя:
«{question}»

Твой ответ:"""

    # Шаг 8: Вызов LLM
    llm = ChatOpenAI(
        api_key="not-needed",
        base_url=str(settings.vllm.generative_base_url),
        model=settings.vllm.generative_model,
        temperature=0.0,  # Максимальная детерминированность
        max_tokens=1024
    )

    resp = await llm.ainvoke([
        ("system", system_prompt),
        ("user", user_prompt)
    ])
    answer = resp.content.strip()

    # Шаг 9: Точная проверка на отсутствие информации
    if answer == "Я не нашёл информацию в документах":
        return "REFLECT: Релевантная информация не найдена"

    return answer